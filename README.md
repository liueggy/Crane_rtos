# Crane_rtos

基于 STM32F103ZET6、FreeRTOS、CubeMX 和 CMake 的起重式散料货物搬运机器人控制工程。

项目面向物流技术创意赛运输豆子任务，目标是让机器人完成自主识别、取料、绕障运输、准确放料和归位。当前工程已具备正式比赛状态机、视觉任务规划、运输动作及故障降级框架，仍需在完整场地上完成最终联调。

> 当前正式版本说明以本节为准。后文按日期保留的调试记录用于追溯硬件标定，
> 其中已经被正式比赛流程替代的遥控按键或页面描述不代表当前行为。

## 正式比赛版本（2026-07-31）

当前软件已经形成可连续执行的正式比赛流程：启动检查、数字区扫描、豆子区扫描、任务规划、三次抓取与放置、强制经过起点绕障、返回起点和最终收尾。比赛启动后，遥控器只保留按键响应蜂鸣，不允许翻页、改参数或中断状态机，避免误操作改变比赛过程。

OLED 仅对外提供五个正式入口：

1. `比赛运行`：默认首页，显示当前任务、阶段、站点、载荷、路线、坐标可信状态；扫描期间实时显示五个数字箱和三个豆子箱结果。
2. `初始化调试`：赛前完成 Z 轴触底/抬升、X 轴回零和初始化动作检查。
3. `底盘调节`：赛前查看两侧运行、光电门、防重复触发状态和四轮平均转速。
4. `静态视觉`：赛前检查 K230 数字/豆子模式及实时识别结果。
5. `故障查询`：查看最近 8 条故障或重试记录，左右键切换记录。

### 视觉降级原则

- 已经稳定识别且不冲突的数字和豆子结果必须保留。
- 缺失、重复或无效结果不能阻塞比赛；规划器从尚未占用的语义和物理箱位中做确定性伪随机补全。
- 数字 1~5 和三种豆子在补全后保持一一对应；三次运输不会选择同一个目标箱。
- K230 模式切换失败或超时在比赛模式下记为识别警告并继续；调试模式仍显示失败，便于赛前排查通信。
- 识别降级只允许替代视觉判断，不能绕过限位、急停、坐标边界或载荷状态检查。

### 故障与恢复

- 坐标仍可信但导航或动作尚未成功启动时，当前阶段最多自动重试 2 次；每次先停止底盘、X/Z 轴和舵机渐变，等待 300ms 后重新进入该阶段。
- 已经进入运动后的导航/抓放故障会使坐标或实际载荷状态不确定，因此立即安全停机，不盲目重复动作。
- 底盘导航按 `8.5s/地标` 设置上限，覆盖 50RPM 下最长约 858mm 路段及两侧停稳对齐时间。
- 重试记录写入 RAM 环形历史，OLED 标记为“已重试”；超过次数后进入终止故障。
- 急停、限位、起始姿态错误、标定缺失、X/Z 坐标错误、载荷状态矛盾和最终回零失败属于硬故障，不使用视觉随机策略掩盖。
- 故障历史仅保存在 RAM，断电后清空；正式运行前仍必须完成机械、光电门和 K230 实机检查。

## 1. 项目状态

当前已经具备：

- 双 TB6600 步进轴的启停、方向和使能控制；X/Z 两轴已接线并完成红外手动控制验证，当前速度合适。
- 四路 L298N 直流电机双 PWM 驱动结构；四轮方向、编码器测速和闭环同步控速已完成实机验证。
- 两路舵机 PWM；PA0 旋转舵机和 PA1 夹爪舵机均已完成关键角度实机标定。
- K230 USART2 DMA通信已启用；K230默认持续发送ASCII识别字符，STM32在到点后开启本地投票窗口。
- OLED 五页全中文状态界面。
- 急停、四路对射光电限位和 EXTI 事件框架。
- FreeRTOS 任务框架、独立红外任务、路线表和比赛状态机骨架。

当前尚未完成：

- 底盘落地承载后的四轮闭环复核与直线偏差标定。
- X/Z 轴双次回零与指定位置闭环。
- K230 视觉结果协议、多帧确认和任务表生成。
- 底盘路线到点、绕障和自动运输实机联调。
- 第二路舵机的机械动作与角度标定。
- 将已验证的夹爪抓取动作接入自动路线和比赛状态机。

自动状态机目前默认停在 `VISION_SCAN`，不会在硬件未接齐时自动驱动机器人跑路线。

### 2026-07-17 舵机与红外调试进度

- PA0 / TIM2_CH1 为 270 度旋转舵机；规定 0 度对应舵机实际 6 度，后续规定角度统一叠加 6 度安装偏移。
- PA1 / TIM2_CH2 为 180 度夹爪舵机；实机确认 `10°` 完全闭合、`45°` 可完全放下豆子、`100°` 是能伸入放置盒的最大张角。
- TIM4_CH4 / PB9 已接入 NEC 红外解码，并新增独立 `IR_REMOTE` FreeRTOS 任务处理所有有效按键。
- 该阶段的夹爪红外演示绑定已经解除，当前红外按键改用于 X/Z 步进轴手动调试，映射见“红外遥控”章节。

### 2026-07-19 双步进轴红外调试进度

- X/Z 两轴均使用 TB6600 的 4 细分，即 `800` 脉冲/圈；TIM1 当前为 `PSC=71`、`ARR=399`，脉冲频率约 `2.5kHz`。
- 运行时 CCR 为 `250`；TB6600 按脉冲边沿计步，占空比不决定转速。按 800 脉冲/圈计算，理论电机转速约 `187.5RPM`。实机确认当前两轴速度合适，暂不提高频率或修改细分。
- 遥控器数字 `1/2` 分别选择 X/Z 轴；`LEFT/RIGHT` 设置 X 轴反/正转，`UP/DOWN` 设置 Z 轴正/反转，`POWER` 启停当前轴。
- 手动调试采用单轴互锁：切换控制轴时先停止原轴，防止两轴意外同时运行。
- 运行中反向时先停止并缓存目标方向，等待 `300ms` 后再恢复；等待期间按 `POWER` 会取消换向并保持停止。
- 急停或任一限位故障存在时，两轴停止且红外命令不能重新启动电机。
- OLED 步进页面已完成中文显示：遥控步进、选择轴、运行状态、方向状态和换向等待。
- 每个通过 NEC 校验的遥控按键都会触发 PB8 蜂鸣器短响约 60ms；采用非阻塞定时关闭，不影响红外解码和换向状态机。安全故障下蜂鸣仍表示按键码已收到，但不会执行电机动作。

### 2026-07-23 全机构遥控与 OLED 调试界面

- 遥控器采用“当前页面决定控制对象”：`VOL-/VOL+` 或 K0/K1 翻页；步进页内 `2/3` 选择 X/Z，舵机页内 `4/5` 选择旋转/夹爪，底盘页内 `6/7/8` 调整底盘。
- 四个直流电机不再支持单轮遥控，底盘始终四轮整体启停和整体换向。
- `VOL- / VOL+` 切换 OLED 上一页/下一页；开机进入总览页。
- `0` 为软件总停止，关闭四轮底盘和 X/Z 步进轴；舵机保持当前位置。
- 切换控制对象时先停止此前选中的底盘或步进轴，避免运动机构在后台继续运行。

## 2. 工程结构

```text
Crane_rtos/
├── Core/Inc/                 头文件和 CubeMX 生成接口
├── Core/Src/                 应用代码与 CubeMX 生成代码
├── Drivers/                  STM32 HAL/CMSIS 驱动
├── Middlewares/              FreeRTOS
├── cmake/                    ARM GCC 和 CubeMX CMake 配置
├── Crane_rtos.ioc            CubeMX 配置文件
├── CMakeLists.txt            应用源文件和构建入口
├── CMakePresets.json         Debug/Release 预设
├── STM32F103XX_FLASH.ld      STM32F103xE 链接脚本
└── README.md                 项目配置、开发进度与移植说明
```

业务代码按模块拆分，`main.c` 主要保留外设初始化、HAL 回调转发和 FreeRTOS 任务入口：

| 模块 | 文件 | 职责 |
| --- | --- | --- |
| 舵机 | `servo_control.c/.h` | 角度、脉宽和限幅 |
| 步进轴 | `stepper_axis.c/.h` | X/Z 轴使能、方向和停止 |
| 底盘 | `chassis_motion.c/.h` | 四轮调速、手动调试和路线段接口 |
| 安全 | `safety_manager.c/.h` | 急停、限位和故障停机 |
| 输入 | `input_manager.c/.h` | 按键消抖和输入事件 |
| K230 | `k230_link.c/.h` | USART2 DMA、命令和视觉接口 |
| 路线 | `robot_routes.c/.h` | 取货、绕障、放料、归位路线表 |
| 状态机 | `robot_controller.c/.h` | 比赛流程控制 |
| 状态/参数 | `app_state.c/.h`、`app_config.c/.h` | 运行状态和统一参数 |

## 3. 开发环境

### 必需工具

| 工具 | 用途 | 要求 |
| --- | --- | --- |
| Git | 克隆与版本管理 | 能执行 `git --version` |
| CMake | 生成构建文件 | 3.22 或更高 |
| Ninja | CMake 构建生成器 | 在 `PATH` 中 |
| GNU Arm Embedded GCC | 编译 STM32 | `arm-none-eabi-gcc` 在 `PATH` 中 |
| STM32CubeMX | 打开/修改 `.ioc` 并生成代码 | 安装任意可用版本，建议与项目生成版本相近 |
| STM32CubeProgrammer | 下载固件 | 仅烧录时需要 |
| ST-LINK 驱动 | SWD 下载/调试 | 仅连接开发板时需要 |

当前工程的 CMake 工具链没有写死 GCC 安装目录，使用以下命令查找工具：

```powershell
arm-none-eabi-gcc --version
cmake --version
ninja --version
```

如果 `arm-none-eabi-gcc` 找不到，需要把 GNU Arm Embedded Toolchain 的 `bin` 目录加入当前用户或系统 `PATH`，然后重新打开终端。

Windows 下可使用 STM32CubeCLT 自带的 GCC。不同电脑的安装目录可能不同，README 不要求固定为某个盘符。

## 4. 克隆后首次构建

在仓库根目录执行：

```powershell
git clone <仓库地址>
cd Crane_rtos
cmake --preset Debug
cmake --build --preset Debug
```

或者使用已经生成的构建目录：

```powershell
cmake --build build\Debug
```

成功后主要产物为：

```text
build/Debug/Crane_rtos.elf
```

Release 构建：

```powershell
cmake --preset Release
cmake --build --preset Release
```

如果首次构建报 `arm-none-eabi-gcc is not recognized`，这是本机工具链 PATH 问题，不是项目源码问题。若报 `ninja is not recognized`，安装 Ninja 并加入 PATH，或在 CMake 中改用本机可用的生成器。

## 5. CubeMX 配置与重新生成

CubeMX 工程文件为：

```text
Crane_rtos.ioc
```

打开后修改引脚、定时器、DMA、FreeRTOS 或 NVIC 配置，再点击 **Generate Code**。生成完成后必须重新构建：

```powershell
cmake --build --preset Debug
```

生成后至少检查：

- `Core/Inc/main.h` 的标签与预期一致。
- `Core/Src/main.c` 中 `MX_TIM8_Init()` 存在。
- TIM3/TIM8 四通道 PWM 存在。
- USART2 DMA RX/TX 和空闲线接收存在。
- `PC5 / LIMIT_Z_MIN` 为 `GPIO_MODE_IT_RISING + GPIO_PULLDOWN`。
- `PE0/PE1/PE2` 同样为高有效限位输入。
- `EXTI9_5_IRQHandler` 同时转发 `LIMIT_Z_MIN_Pin` 和 `ESTOP_IN_Pin`。

CubeMX 可能重写生成区域，因此手写业务应放在 `USER CODE` 区域或独立 `.c/.h` 文件。修改 `.ioc` 后不要只修改生成的 `main.c`，否则下次生成可能丢失配置。

## 6. 烧录与调试

项目当前没有把 STM32CubeProgrammer 或 ST-LINK 的绝对路径写入 CMake，也没有固定烧录脚本。不同电脑只需要在本机安装工具并替换命令路径。

推荐使用 SWD：

| ST-LINK | STM32 |
| --- | --- |
| SWDIO | PA13 |
| SWCLK | PA14 |
| GND | GND |
| 3.3V | 目标板 3.3V 检测/供电，按调试器要求连接 |
| NRST | NRST，可选但建议连接 |

生成 Intel HEX 或 BIN：

```powershell
arm-none-eabi-objcopy -O ihex build\Debug\Crane_rtos.elf build\Debug\Crane_rtos.hex
arm-none-eabi-objcopy -O binary build\Debug\Crane_rtos.elf build\Debug\Crane_rtos.bin
```

使用 STM32CubeProgrammer CLI 烧录时，命令格式通常为：

```powershell
STM32_Programmer_CLI.exe -c port=SWD -w build\Debug\Crane_rtos.elf -v -rst
```

如果命令找不到，请使用本机 STM32CubeProgrammer 安装目录中的 `STM32_Programmer_CLI.exe` 的绝对路径。也可以使用 STM32CubeProgrammer 图形界面选择 `Crane_rtos.elf`、连接方式 `ST-LINK/SWD` 后下载。

烧录前确认：目标板供电正常、ST-LINK 电压与目标板一致、所有电机驱动器处于安全停止状态。

## 7. 硬件基线

### 直流电机与编码器

四路直流电机由集成两颗 L298N 的四路驱动板控制。电机使用 12V，编码器统一使用 STM32 的 3.3V。接线或改线前必须同时关闭 12V 电机电源和控制板电源。

#### 电源与共地

```text
12V电源正极  -> L298N VCC / 电机电源+
12V电源负极  -> L298N GND
STM32 GND     -> L298N GND
编码器黑线    -> STM32 GND
```

12V 电源负极、L298N GND、STM32 GND 和四个编码器黑线必须共地。模块自带的 5V 输出暂不连接 STM32，避免与控制板自身电源互相倒灌。编码器蓝线严禁接入 12V。

#### 电机输出与控制输入

| 电机 | STM32 PWM 1 | L298N输入 | STM32 PWM 2 | L298N输入 | 电机红/白线 |
| --- | --- | --- | --- | --- | --- |
| M1 | PA6 / TIM3_CH1 | IN1 | PA7 / TIM3_CH2 | IN2 | OUT1 / OUT2 |
| M2 | PB0 / TIM3_CH3 | IN3 | PB1 / TIM3_CH4 | IN4 | OUT3 / OUT4 |
| M3 | PC6 / TIM8_CH1 | IN5 | PC7 / TIM8_CH2 | IN6 | OUT5 / OUT6 |
| M4 | PC8 / TIM8_CH3 | IN7 | PC9 / TIM8_CH4 | IN8 | OUT7 / OUT8 |

L298N 的 EN1 至 EN4 只保持使能，不承担 PWM。如果驱动板带 EN 跳帽，调试时保留跳帽；如果是独立 EN 输入，则按照模块说明置于有效电平。当前软件采用双 PWM 控制：

```text
正转：IN1=PWM，IN2=0
反转：IN1=0，IN2=PWM
停止：IN1=0，IN2=0
```

TIM3 控制 M1/M2，TIM8 控制 M3/M4，两者当前均为约 10kHz PWM。STM32 的 3.3V 高电平满足该模块标注的 2.3V 以上有效输入要求。

#### 编码器接线

每个电机的六根线定义：

```text
红线 -> 电机端，接对应 OUT 奇数端
白线 -> 电机端，接对应 OUT 偶数端
蓝线 -> STM32 3.3V
黑线 -> STM32 GND
黄线 -> 编码器 A 相
绿线 -> 编码器 B 相
```

| 电机 | 黄线 A | 绿线 B | 蓝线 | 黑线 |
| --- | --- | --- | --- | --- |
| M1 | PD10 | PD4 | 3.3V | GND |
| M2 | PD13 | PD5 | 3.3V | GND |
| M3 | PD14 | PD6 | 3.3V | GND |
| M4 | PD15 | PD7 | 3.3V | GND |

编码器 A 相使用上升沿 EXTI，B 相作为方向判定输入，两者均启用内部上拉。驱动轮实测周长为`263.9mm`，统一保存在`AppConfig.wheel_circumference_mm`。2026-07-28底盘直线复标将输出轴计数修正为`468计数/圈`，对应理论分辨率约`263.9/468=0.5639mm/计数`。复测结果如下：

- `1000mm / 40RPM`：M1=`900mm`、M2=`1055mm`、M3=`1078mm`、M4=`1004mm`，四轮平均约`1009mm`。
- `2000mm / 40RPM`：M1=`1848mm`、M2=`1995mm`、M3=`2132mm`、M4=`2042mm`，四轮平均约`2004mm`。
- `110RPM`下实测`1000mm`和`2000mm`的总位移均可控制在目标上下约`100mm`内，包含惯性停车距离。

因此当前统一里程比例暂定为`468计数/圈`。M1计数持续偏低、M3持续偏高，后续应结合底盘终点横向偏移和航向角判断是单轮编码器比例差、轮径差还是打滑；在证据明确前不要仅按四轮计数差直接修改PI。正式分段路线不使用编码器里程决定停车，编码器只负责闭环同步和宽松行程校验；所有Y地标均以两侧光电门独立触发为界。

#### 电流、散热与首次上电

驱动板标注的 2A 通常指单路电机桥的短时峰值，不是四路模块总共只有 2A，也不代表四路可以长期各输出 2A。建议将单路连续电流控制在约 0.8～1.2A 范围内，具体上限取决于散热片、风冷和 PCB 走线。四路总电流还受 12V 电源额定电流限制。

首次上电按以下顺序进行：

1. 将车架架空，确保四个轮子均不接触地面。
2. 断电状态下检查 12V 与 3.3V 之间不存在短路。
3. 只连接 M1，电源设为 12V、约 1A 限流，验证正反转和编码器计数。
4. 依次接入 M2、M3、M4，每增加一路都检查空载电流和 L298N 温升。
5. 四路空载正常后再逐步提高总限流，12V/5A 电源最大只能提供约 5A 总电流。
6. 确认急停、停止命令和软件 PWM 均有效后，再让轮子落地低速测试。

如果某个轮子的正转方向与整车定义相反，优先调整 `motor_polarity[]`，也可以断电后交换该电机红白线。编码器方向错误时再交换黄绿线或调整编码器极性，避免一次同时改变电机线和编码器线而难以定位问题。

当前实机规定正方向已经标定：M1、M2 的安装方向与 M3、M4 相反，因此固定使用 `motor_polarity = {-1, -1, 1, 1}`。上层底盘和路线代码只使用统一的正负速度定义，不应再次针对 M1/M2 单独交换方向。

2026-07-23 串口闭环遥测确认：开环规定正向下 M1/M2 原始编码器计数为负，因此反馈配置固定为 `encoder_polarity = {-1, -1, 1, 1}`。同时实机的 M3/M4 驱动线序与最初逻辑标号交叉，软件已交换 TIM8_CH1/2 与 TIM8_CH3/4 的逻辑映射，使 P3 对应编码器3、P4对应编码器4。该修正不改变四轮已经验证正确的开环物理正方向。

### 步进轴

| 轴 | PUL | DIR | ENA |
| --- | --- | --- | --- |
| X | PE9 / TIM1_CH1 | PD2 | PD11 |
| Z | PE11 / TIM1_CH2 | PD3 | PD12 |

TB6600 ENA 低电平有效。X/Z 两轴当前均使用 4 细分、800 脉冲/圈；TIM1 `PSC=71`、`ARR=399`，约 2.5kHz，运行 CCR 为 250。理论转速约 187.5RPM，当前实机速度已确认合适。

### 世界坐标约定（2026-07-27确认）

- 世界XY原点位于场地图示右上角。
- `+X`沿场地横向向左；X轴步进电机正转时夹爪沿`+X`移动。
- `+Y`沿场地纵向由豆子区指向数字区；底盘规定正转时沿`+Y`移动。
- `+Z`竖直向下；Z轴步进电机正转时夹爪下降。
- X轴右端定义为`X=0`，左端约为`X=33924`脉冲。
- Z轴最高安全位置定义为`Z=0`，比旧最高点再提高约15mm；触底光电门位置为`Z=6093`脉冲，触底点是Z最大值。
- 软件中步进轴`reverse=0`表示沿坐标正方向运动，`reverse=1`表示沿坐标负方向运动；带符号脉冲累计遵循同一规则。
- 底盘四轮的安装和编码器极性已在驱动层归一化，上层路线使用正速度表示`+Y`，负速度表示`-Y`。
- 世界坐标、机构坐标和摄像头像素坐标必须分开保存，K230画面横坐标不能直接作为龙门X轴坐标使用。

Z轴回零时先向下低速寻找触底光电门，将该点标记为`Z=6093`，随后向上运行约296mm的已标定行程到达新`Z=0`；光电硬限位仍具有最高优先级。

### 世界地图与自动任务框架

世界地图采用固定场地的“度量坐标+离散Y地标”模型。实物现已安装完成9组光电定位挡板，从豆子区沿`+Y`到数字区依次排列；左右底盘各有一个光电门，每组地标两侧各一片挡板。单侧触发只锁存本组事件，只有两侧均触发才算整车完成该地标确认；目标点随后让四轮同时停车。A/C夹取挡板同时是豆子端机械允许的最上极限，2/3/4号箱挡板同时是数字端最终定位，地图不再额外定义不存在的顶端或底端边界挡板。

| Y向顺序 | 挡光片/地标职责 | 主要动作 |
| --- | --- | --- |
| A/C夹取地标（上端极限） | A、C两个豆子箱共用Y定位，也是底盘向豆子端运动的最终挡板 | X轴分别移动到A或C后夹取，禁止继续向上 |
| B夹取地标 | 中间凸出B豆子箱Y定位 | X轴移动到B后夹取 |
| 上障碍上侧/豆子识别地标 | 上方障碍物靠豆子区边界，同时是摄像头采集豆子信息的停车点 | 障碍物框定、豆子识别 |
| 上障碍下侧 | 上方障碍物靠起点侧边界 | 路线避障 |
| 起点地标 | 两个障碍物之间的比赛起始位置 | 启动、正式搬运中心通道 |
| 下障碍上侧 | 下方障碍物靠起点侧边界 | 路线避障 |
| 下障碍下侧/数字识别地标 | 下方障碍物靠数字区边界，同时是摄像头采集数字信息的停车点 | 障碍物框定、数字识别 |
| 1/5号箱放置地标 | 两侧竖放的1、5号箱共用Y定位 | X轴到对应侧端后放豆 |
| 2/3/4号箱放置地标（下端最终定位） | 底部横排2、3、4号箱共用Y定位，也是底盘向数字端运动的最终挡板 | 旋转舵机90度后按X坐标放豆，禁止继续向下 |

这里的A/B/C和1~5表示当前图中的物理箱位；比赛抽签及K230识别完成后，还需建立“豆子种类/数字语义 -> 物理箱位”的映射。图示确认了全部9组挡板的拓扑和用途，但不用于估算未测量的Y距离；各地标的编码器距离、减速点和停车补偿仍需实机标定。A/C与B、1/5与2/3/4均是相邻但不同的Y地标。

2026-07-28沿场地一侧从豆子端向数字端实测得到七段连续挡光片间距：

| 相邻Y地标 | 实测距离 | 毫米值 | 按468计数/圈的理论增量 |
| --- | ---: | ---: | ---: |
| A/C夹取 → B夹取 | 25.7cm | 257mm | 约456计数 |
| B夹取 → 上障碍上侧/豆子识别 | 32.7cm | 327mm | 约580计数 |
| 上障碍上侧/豆子识别 → 上障碍下侧 | 61.1cm | 611mm | 约1084计数 |
| 上障碍下侧 → 起点 | 85.8cm | 858mm | 约1522计数 |
| 起点 → 下障碍上侧 | 40.7cm | 407mm | 约722计数 |
| 下障碍上侧 → 下障碍下侧/数字识别 | 59.0cm | 590mm | 约1046计数 |
| 下障碍下侧/数字识别 → 1/5号箱 | 43.5cm | 435mm | 约771计数 |
| 1/5号箱 → 2/3/4号箱 | 已安装、待测 | 待标定 | 待标定 |

已测七段合计`3485mm`，理论约`6180`个编码器计数；第8段“1/5号箱→2/3/4号箱”挡板已经安装完成，但距离尚待实测。主识别和跨障碍搬运路线已经按七段实测值写入`robot_routes`，并按路程、速度和3秒余量生成超时。A/C和2/3/4已经分别作为9组地图的首末地标，不再等待额外端部边界距离。正式路线只用已知距离确定方向、估算超时并检查行程；不会在编码器达到理论距离时停车或进入下一段。

新增模块：

- `world_map.c/.h`：世界站点、8个箱位、A/B/C扫描姿态、XYZ坐标有效性和统一标定接口。
- `vision_survey.c/.h`：保存A/B/C识别结果，检查数字1~5唯一性，并在识别两种不同豆子后用排除法补齐第三种。
- `mission_planner.c/.h`：按比赛规则生成黄豆到1号、绿豆到2号、白芸豆到3号的三条搬运任务。
- `robot_routes.c/.h`：保存路线拓扑和逐段实测参数。
- `route_executor.c/.h`：非阻塞执行底盘路线段，并在到站后修正世界Y站点。
- `robot_controller.c/.h`：启动、识别、建表、抓取、运输、放置和完成的顶层状态机。

路线分为两类：

- 识别阶段：起点到数字识别点、数字识别点到豆子识别点使用`DIRECT_SURVEY`路线，允许沿标定侧直行，不要求在障碍之间交叉换边。
- 正式搬运阶段：豆子区与数字区往返使用`REQUIRE_CENTER_PASS`路线，拓扑中必须包含`WORLD_STATION_START`；缺少中心节点时路线执行器拒绝启动。

底盘速度和双侧光电允许时差现在是全局联调参数，默认分别为`50 RPM`和`1000ms`。底盘控制页按`7/8`以`5 RPM`步长减小/增大速度，按`4/5`以`100ms`步长减小/增大允许时差；可调范围分别为`20~130 RPM`和`100~3000ms`。普通路线、B-C-A、XY导航和视觉路线启动时均读取这两个全局值，不再用模块内的固定50 RPM覆盖手动设置。路线总超时会按“标称速度/当前全局速度”同比缩放，降低速度不会沿用原高速超时而提前报错。左右光电门独立锁存同组事件但不单边停车，两侧都遮挡后才在目标点同时停止四轮。

凸出B箱`WORLD_STATION_BEAN_B_PICK`和数字侧箱1/5共用的`WORLD_STATION_NUMBER_SIDE_15`属于易受惯性影响的精确地标。正式比赛、XY导航和路线执行器把它们作为目标站时，仍按全局速度直接到站，不提前减速；双侧目标事件触发后四轮同时清零，等待`300ms`让车体停稳，再检查PD8/PB12当前遮挡。如果任意一侧已滑出挡板，底盘以`10 RPM`先沿来向反方向微调，当前已遮挡侧保持停止，未遮挡侧进入遮挡便单独停车。再次停稳后若惯性使某侧从挡板另一边滑出，则切换方向只调整未遮挡侧，如此往返，直到PD8/PB12当前同时被遮挡才上报路线完成并更新Y站点。往返微调设`5s`总超时，失败时停止运动并令Y坐标失效。上述地标仅在作为最终目标时启用修正，中途经过仍只计数并连续行驶。

导航器显式区分`EMPTY/LOADED/UNKNOWN`载荷。载豆跨区时强制选择左右不同的入口、出口安全通道，保证货物轨迹在两障碍之间换边；空爪跨区则比较左右通道代价，同侧箱沿同一侧最短直达，异侧箱才在中间换边，B箱和数字3号箱等中线点自动选择总代价更低的一侧。若当前X位于中线危险带，底盘启动前先把X预对齐到选定入口安全位。载荷未知时禁止自动导航。

需要中间换边时，PD8/PB12两侧都通过靠近起点的安全直道入口挡板，并且两侧光束恢复无遮挡连续稳定`100ms`后，底盘保持当前速度继续向起点行驶，同时启动X。中途挡板不停车、不清零速度环，X也不会与光电边沿在同一控制周期突然启动。底盘到达起点和X换边完成是两个并行互锁条件：任一条件未完成都不会驶入另一障碍区；若X较慢，底盘会在起点等待。

自动启动采用“先清限位、再快速建立绝对坐标”的顺序。比赛摆放时软件将X初始化为中点`16962`、Z初始化为触底`6093`；按约296mm全行程换算，夹爪先向上约`1544`脉冲，到达离地约75mm、绝对`Z≈4549`并离开PB11。随后X向右端寻找PB11并建立`X=0`，再沿`+X`移动`8500`脉冲对准物理2号箱。

识别任务完成并建立搬运表后，控制器把Z抬到抓取预备高度`Z=2609`，比B箱抓取高度`Z=2809`再高200脉冲；坐标整体增加309脉冲，实物高度不变。

比赛起点上电时Z触底会遮挡这一个共用光电门，此时X轴必须位于不遮挡的位置。自动启动只在已知起点状态下调用`StepperAxis_ArmPhotoLimitEscape(STEPPER_AXIS_Z, 0)`，将“向下”记录为触发方向，只允许Z轴反转向上脱离；继续向下和X轴启动仍被互锁禁止。确认光束恢复后，才执行后续X轴正向偏移动作。

所有未知参数默认保持未标定，不能用0代替。调用`RobotController_RequestStart()`后，状态机先检查：

- 各挡板站点的世界Y坐标；
- 8个箱位的X脉冲和抓放Z脉冲；
- A/B/C的X、Z、旋转角、俯仰脉宽和稳定时间；
- 两条识别路线、两条强制中心搬运路线及结束路线的每一段距离、转角、速度和超时。

任一参数缺失时进入`ROBOT_STATE_CALIBRATION_REQUIRED`，不会启动步进或底盘。可通过`RobotController_GetMissingCalibrationMask()`查看缺失类别。后续建议按“站点Y坐标 -> 识别姿态 -> 箱位X/Z -> 直行识别路线 -> 中心搬运路线 -> 完整抓放”的顺序逐项写入标定值。

### 舵机、OLED、K230

- 旋转舵机：PA0 / TIM2_CH1，物理范围 0～270 度，对应 500～2500μs；规定角度自动增加 6 度安装偏移，上电和遥控复位显示为规定 0 度，实际输出 6 度。
- 夹爪舵机：PA1 / TIM2_CH2，范围 0～180 度，对应 1000～2000μs；闭合/放豆/最大张角分别为 10/45/100 度，上电和遥控复位到闭合的 10 度。
- 两路舵机遥控 UP/DOWN 每次增减 10 度。旋转规定角限制为 0～264 度，夹爪工作角限制为 10～100 度，超过边界的命令自动夹紧。两路均为 50Hz，并使用独立 5V/6V 电源。
- OLED：PB6/PB7 / I2C1，SSD1306，常用地址 0x3C。
- K230：PA2 为 USART2_TX，PA3 为 USART2_RX，115200 8N1，DMA RX/TX。当前 `K230_LINK_RUNTIME_ENABLED=1U`，DMA持续接收并排空字符流；STM32不再发送单帧或流式识别请求。

### 按键、急停、对射光电

- K0：PE4，低有效下降沿；K1：PE3，低有效下降沿；急停：PE7，低有效下降沿。
- K0/K1 当前只切换 OLED 上一页/下一页，不直接驱动执行机构；当前运动调试统一通过红外遥控完成。
- X 最小/最大：PE0/PE1；Z 上/下：PE2/PC5。
- 光电模块无遮挡 `DO=低`、遮挡 `DO=高`，四路限位使用内部下拉和上升沿 EXTI。
- 光电模块只有在 DO 高电平不超过 3.3V 时才能直接连接 STM32；高于 3.3V 必须做电平转换。

### 红外遥控

- 接收端 DO：PB9 / TIM4_CH4，标签 `IR_REMOTE_RX`；应连接 3.3V 逻辑电平的解调型红外接收模块，且必须与 STM32 共地。
- TIM4：CH4 在 CubeMX 中配置为上升沿输入捕获、`PSC=71`、`ARR=65535`，计数分辨率为 1 us；软件在每次捕获后交替切换边沿以完成 NEC 解码。必须启用 `TIM4_IRQn`，不要将其再用作 HAL 时基。

| 按键 | NEC 命令码 | 当前功能 |
| --- | ---: | --- |
| 1 | `0x16` | 当前保留，不跨页面切换机构 |
| 2 | `0x19` | 仅在步进页选择 X 轴 |
| 3 | `0x0D` | 仅在步进页选择 Z 轴 |
| 4 | `0x0C` | 仅在舵机页选择旋转舵机（PA0） |
| 5 | `0x18` | 仅在舵机页选择夹爪舵机（PA1） |
| 6 | `0x5E` | 仅在底盘页且停止时切换开环/四轮闭环 |
| 7 / 8 | `0x08` / `0x1C` | 仅在底盘页将闭环目标转速减/增 10 RPM |
| UP | `0x46` | 底盘/步进设正向，舵机增加 10 度 |
| DOWN | `0x15` | 底盘/步进设反向，舵机减少 10 度 |
| POWER | `0x45` | 底盘/步进启停；旋转舵机回规定0度，夹爪回闭合10度 |
| 0 | `0x42` | 软件总停止：底盘和双步进轴停止 |
| VOL- | `0x07` | OLED 上一页 |
| VOL+ | `0x09` | OLED 下一页 |

UP、DOWN和POWER只作用于当前OLED页面；总览页和系统页不响应机构动作。离开底盘或步进页面时先停止原机构，防止后台继续运动。底盘遥控占空比固定为 `75/99`。底盘和步进电机只有在停止状态下才能改变方向；步进运行中收到反向命令时仍采用300ms停机死区。急停和限位故障优先级最高，故障状态仅允许OLED翻页，不执行运动命令。

底盘默认处于 `CLOSED` 闭环模式，默认目标速度为 `110 RPM`，但上电保持停止，收到明确启动命令后才运动。按 `6` 可在底盘停止时切换开环/闭环，按 `7/8` 调整目标转速。闭环模式使用四轮独立 PI、速度前馈和平均速度同步补偿；开环模式仅用于检查驱动、编码器计数和物理方向。

四轮测速仍以 10ms 周期执行控制，但将编码器增量累计 50ms 后再计算 RPM，降低低脉冲数造成的显示跳变。闭环停止命令会立即将四路 PWM 清零；同向速度控制禁止跨零输出反向制动力，避免某轮超调时突然反转。现阶段不加入 D 项，因为编码器量化噪声会被微分项显著放大。

2026-07-23 实机在线调参结果：`KP=0.8`、`KI=2.5`、`FF=0.65`、`SYNC=0.2` 保持不变，测速滤波系数由 `0.35` 优化为 `0.25`。在架空空载测试中，40/60/80/100/120 RPM 均可稳定跟踪；40 RPM 时四轮平均瞬时离散度约 `0.88 RPM`，120 RPM 时约 `2.03 RPM`。这些参数是空载基线，底盘落地承载后仍需重新验证直线偏移和抗扰性能。

以下 USART2 遥测和在线调参说明属于早期调试协议；当前USART2已专用于K230 ASCII视觉字符，不应再向同一串口混发CSV文本。

启用后，USART2 可按 115200 8N1 输出 CSV：

```text
CSV:M,tick,closed,run,dir,target,r1,r2,r3,r4,p1,p2,p3,p4,c1,c2,c3,c4
M,12340,1,1,0,40,39,41,38,40,52,49,55,51,1024,1010,998,1018
```

`r1~r4` 为滤波后的整数 RPM，`p1~p4` 为控制器实际输出命令，`c1~c4` 为累计编码器计数。遥测由低优先级 UART 任务以约 20 Hz 格式化，并使用非阻塞 DMA 投递；串口忙时丢弃本帧，不占用 10ms 高优先级电机控制任务。

### 串口在线调参

USART2 使用 `115200 8N1`。在线参数只保存在 RAM 中，复位后恢复 `app_config.c` 默认值。所有命令以回车或换行结束：

| 命令 | 作用 |
| --- | --- |
| `MGET` | 查询当前四轮参数、目标转速、模式、启停和方向 |
| `MSET KP 1 0.8` | 修改 1 号电机比例增益；电机号可用 `1~4` 或 `ALL` |
| `MSET KI ALL 1.2` | 修改四轮积分增益 |
| `MSET FF ALL 1.5` | 修改四轮速度前馈 |
| `MSET SYNC ALL 0.2` | 修改四轮平均速度同步增益 |
| `MSET FILTER ALL 0.25` | 修改测速低通滤波系数，范围 `0.01~1.0` |
| `MTARGET 40` | 设置闭环目标转速，范围 `20~130 RPM` |
| `MMODE OPEN` / `MMODE CLOSED` | 切换开环/闭环，要求底盘已经停止 |
| `MDIR FWD` / `MDIR REV` | 切换规定正向/反向，要求底盘已经停止 |
| `MRUN 1` / `MRUN 0` | 四轮启动/停止 |

电脑端工具为 `tools/motor_tuner.py`，依赖 `pyserial`。默认使用 `COM14`：

```powershell
python -m pip install pyserial
python tools/motor_tuner.py --port COM14 send "MGET"
python tools/motor_tuner.py --port COM14 monitor --duration 10 --output telemetry.csv
python tools/motor_tuner.py --port COM14 step --target 40 --duration 12 --settle 3 --output motor_test.csv
```

`monitor` 只读取数据，不会启动电机。`step` 是明确的运动测试：先停止、配置模式/方向/目标，再启动；正常结束、Ctrl+C 或异常退出时都会请求 `MRUN 0`。测试前仍应架空四轮并准备硬件急停，串口断线时不能把软件停机当作唯一保护。

OLED操作页面包括：总览、底盘、距离标定、步进手动、定量脉冲、箱位标定、Z高度标定、回零控制、抓豆控制、放豆演示、舵机、视觉和系统。页面只显示状态并按当前页隔离遥控操作，不会跨页面误控机构。

### 底盘距离标定页

“距离标定”页用于验证车轮周长和编码器每圈计数。默认目标为`500mm / 40RPM`，底盘采用四轮闭环同步控制；启动时记录四路编码器累计计数，以四轮绝对增量平均值达到理论目标计数时自动停车。页面中的“计”表示由编码器参数换算出的距离，“差”表示四轮中最大与最小里程之差，二者均显示为整数毫米，内部计算仍保留0.1mm精度。

| 遥控按键 | 功能 |
| --- | --- |
| `LEFT` | 选择并清空目标距离，随后用数字键输入毫米值，范围1~9999mm |
| `RIGHT` | 选择并清空目标速度，随后用数字键输入RPM，范围20~当前最大RPM |
| `UP/DOWN` | 选择底盘正向/反向 |
| `POWER` | 启动；运行中再次按下立即停止 |
| `VOL-/VOL+` | 离开页面并停止当前标定 |

该页面按调试要求临时忽略PD8/PB12的初始遮挡和运行途中的所有挡片，只根据编码器距离停车；退出、完成、手动停止、超时、急停或故障后自动恢复正常光电停车。测试必须选择无碰撞风险的直线路径并准备硬件急停，不能把场地挡片当作本页的安全限位。

程序停车后用卷尺测量底盘实际距离`D_actual`，与输入目标`D_target`比较。若保持车轮周长`263.9mm`不变，建议编码器每圈计数为：

```text
counts_per_rev_new = counts_per_rev_old * D_target / D_actual
```

例如输入1000mm、当前374计数/圈，实测行驶1050mm，则新值约为`374*1000/1050=356.2计数/圈`。建议正向和反向各测试至少三次，使用实际距离平均值计算；如果四轮“差”持续较大，应先检查轮胎打滑、编码器极性、机械阻力和闭环同步参数，而不是直接修改周长或每圈计数。

步进手动页用于距离标定：`RIGHT`切换X/Z轴，`UP/DOWN`选择方向，`POWER`启停，停止后按`0`清零当前轴带符号累计脉冲。正向脉冲累计为正，反向累计为负。

定量脉冲页用于指定步数测试：数字键逐位输入目标脉冲，`LEFT`删除末位，`RIGHT`切换X/Z轴，`UP/DOWN`选择方向，`POWER`启动或中途停止。运行期间禁止修改目标、轴和方向，页面显示目标与实际完成脉冲。

箱位标定页用于记录五个数字箱物理槽位的X轴坐标。槽位1~3依次为底部左、中、右，槽位4~5为两侧凸出箱位的左、右。进入本页前必须确认Z轴挡片已经离开共用PB11光电门，且标定过程中禁止Z轴运动。

1. 按`LEFT`让X轴向负方向点动，直到右端光电门触发并由软件记录为X轴限位。
2. 保持光电门遮挡时按`0`，将当前位置建立为`X=0`；若当前限位归属于Z轴，清零会被拒绝。
3. 按`RIGHT`沿X正方向点动，`UP/DOWN`在10、50、100、500、1000脉冲五档之间调整单次步长。
4. 按`1~5`选择物理槽位，将夹爪中心对准对应箱体中心后按`POWER`保存当前X坐标。
5. 页面中的`零:已/未`表示X参考点是否有效，`存:已/未`表示当前所选槽位是否已经保存。

建立零点后，点动范围受`STEPPER_X_SAFE_TRAVEL_PULSES`软限位约束。通过标定页重新保存的试验值只在本次运行的世界地图对象中生效；已经确认的正式数值写在`app_config.h`，重新编译后会随固件长期保留。

2026-07-27 已完成数字区五个物理箱位的X轴标定，并写入`app_config.h`编译期配置：

| 物理箱位 | X轴位置/脉冲 | 定位依据 |
| --- | ---: | --- |
| 1，底部左 | 24400 | 夹爪中心对准箱体中心 |
| 2，底部中 | 16300 | 夹爪中心对准箱体中心 |
| 3，底部右 | 8500 | 夹爪中心对准箱体中心 |
| 4，侧面左 | 33924 | 左端光电门刚好遮挡 |
| 5，侧面右 | 0 | 右端光电门刚好遮挡，X轴零点 |

上述编号只描述物理槽位，不代表比赛抽签后的数字1~5。K230完成识别后，应建立“数字语义 -> 物理槽位 -> X轴脉冲”的映射。五个X值上电后会自动装入世界地图，但X轴必须先在右端光电门完成回零，坐标才具有绝对意义；五个箱位的抓放Z高度仍未标定。

2026-07-27 已完成豆子区三个物理箱位的X轴标定：

| 物理豆子箱位 | X轴位置/脉冲 | 定位依据 |
| --- | ---: | --- |
| 顶部左箱 | 26500 | 夹爪中心对准箱体中心 |
| 中间凸出箱 | 16300 | 与数字区底部中箱共用X坐标 |
| 顶部右箱 | 6700 | 夹爪中心对准箱体中心 |

豆子种类与物理箱位的关系由比赛抽签决定。K230在C点识别两种豆子后，通过相对位置和排除法建立“豆子种类 -> 物理箱位 -> X轴脉冲”映射；三个豆子箱的抓取Z高度仍需实机标定。

### Z轴抓放高度标定

Z高度标定页用于测量三档递增夹取高度和一个放豆高度。Z坐标以新最高位置为`0`、向下为正，夹爪触底光电门位置为`6093`。进入本页前必须确认X轴挡片没有遮挡共用PB11光电门，标定过程中X/Z仍保持互斥。

| 遥控按键 | 功能 |
| --- | --- |
| `1/2/3` | 选择夹取高度1、2、3 |
| `4` | 选择放豆高度 |
| `UP` | 夹爪向上点动，Z坐标减小 |
| `DOWN` | 夹爪向下点动，Z坐标增大 |
| `LEFT/RIGHT` | 减小/增大点动步数 |
| `0` | 在Z触底光电门遮挡时建立`Z=6093`参考，并自动上升完整行程回到`Z=0` |
| `POWER` | 保存当前所选高度 |

点动步数分为`10/50/100/500/1000`五档，默认100脉冲。先让Z轴低速向下触发光电门并按`0`建立参考，程序会自动向上运行6093脉冲回到最高位置`Z=0`；自动回顶期间页面显示`底:上升`。

2026-07-27 已完成Z轴抓放高度标定：

| 标定项目 | Z轴位置/脉冲 |
| --- | ---: |
| 夹取高度1，中间凸出豆子箱 | 2809 |
| 夹取高度2，顶部右侧豆子箱 | 3599 |
| 夹取高度3，顶部左侧A豆子箱 | 4299 |
| 数字箱放豆高度 | 3609 |

四个值已经写入`app_config.h`，相对旧坐标整体增加309脉冲，因此实物抓放高度不变。五个数字箱的`action_z_pulses`统一初始化为3609。

### 槽位旋转姿态

世界地图的每个箱位带有`tool_yaw_degrees`规定逻辑角度。三个豆子箱在夹取前均使用初始`0°`姿态；数字区底部左、中、右三个横排箱在下降放豆前先旋转到`90°`；数字区两侧凸出箱保持初始`0°`，无需旋转。动作顺序必须是“Z轴处于安全高度 -> 旋转舵机到槽位角度 -> 等待稳定 -> Z轴下降 -> 夹爪释放 -> Z轴抬升”，禁止在夹爪已经伸入箱体后旋转。

地图保存的是规定逻辑角度，旋转舵机驱动会自动叠加已标定的`6°`安装偏移，因此上层不得把90度手工改成96度。离开一个箱位并抬升到安全高度后，应将旋转舵机恢复规定`0°`，再进行下一段底盘或X轴运动。

### 初始化调试页

“回零控制”页提供一键初始化调试。规定启动姿态为底盘停在场地起点，PD8和PB12均被起点两侧挡片遮挡，X轴夹爪位于横梁中点且Z轴触底。两路底盘光电门的高电平在该姿态下属于正常现象，本流程不会启动底盘，也不使用它们判断X/Z位置。

按`POWER`后依次执行：将Z触底点记为`6093` -> 上升6093脉冲到新`Z=0` -> X建立右端`X=0` -> X回中点16962 -> Z再次触底并恢复`Z=6093`。Z轴停止后继续保持使能。

- `POWER`：待机、完成或故障状态下开始；运行中再次按下会立即中止。
- `0`：立即中止并停止X/Z脉冲，Z轴仍按正常策略保持使能。
- `VOL-/VOL+`：离开页面时自动中止，防止调试动作在后台继续。
- 每个找限位或定量移动阶段均带独立超时；PB11没有在预期时间触发、有限脉冲提前中断、安全限位或急停都会进入“故障”。

X/Z共用PB11，因此流程严格串行，任何时刻只允许一个步进轴运动。最终回到X中点后才允许Z下降触底，避免X端部挡片与Z触底挡片同时遮住光束。该流程只适用于上述已知起始姿态；如果X实际停在端部却把PB11遮挡误认为Z触底，会破坏限位归属，必须先人工恢复到规定姿态。

### 三位置抓豆调试页

“抓豆控制”页用于在操作者已经手动对齐底盘Y位置后，分别验证三个豆子物理箱位的抓取动作。完成“回零控制”流程后进入本页，首次按`POWER`会先让Z上升到最高点`Z=0`，再让X向右寻找PB11遮板并把触发点定义为`X=0`。由于X/Z共用PB11，建立零点后X会自动沿`+X`退出500脉冲以释放光电门，随后页面显示`READY`。

| 遥控按键 | 功能 |
| --- | --- |
| `1` | 选择顶部左箱：X=26500、Z=4299 |
| `2` | 选择中间凸出箱：X=16300、Z=2809 |
| `3` | 选择顶部右箱：X=6700、Z=3599 |
| `POWER` | 尚未READY时建立抓豆起始点；READY后执行当前所选位置的完整抓取动作 |
| `0` | 立即中止X/Z运动；夹爪保持当前位置，避免已夹住的豆子被释放 |
| `VOL-/VOL+`或板载翻页键 | 中止后切换页面 |

抓取动作顺序为：Z轴先回到新最高位置Z=0 -> 旋转舵机回规定0度并把夹爪张开到100度 -> X轴移动到标定位置 -> Z轴下降到对应抓取高度（左4299、中2809、右3599）-> 夹爪闭合到10度并保持900ms -> Z轴重新抬升到Z=0。三个坐标相对旧坐标整体增加309脉冲，实物抓取高度不变。

### B-C-A完整抓取稳定性Demo

“抓取稳定测试”页用于从比赛起点规定姿态依次验证中间凸出B箱、后排C箱和A箱。启动前必须满足：底盘位于起点两侧挡板、X轴在横梁中点、Z轴触底并遮挡PB11。遥控器按`POWER`启动，按`0`或离开页面立即停止底盘和X/Z轴。

启动后先让Z从触底位置升到`Z=0`，再让X寻找右端PB11、建立`X=0`并退出500脉冲。随后底盘使用“底盘控制”页当前设置的全局速度沿安全侧向豆子区运行，连续经过3组挡板到达B箱定位地标，总参考距离为`858+611+327=1796mm`。B目标挡板双侧触发后四轮同时停车并等待300ms；若惯性使任意一侧滑出挡板，则以`10 RPM`往返微调，分别让PD8/PB12进入遮挡并停稳，只有当前两侧都被遮挡才确认到站并开始抓B。往返微调总超时为5秒，失败立即安全停机。

B箱按`X=16300、Z=2809`抓取，C箱按`X=6700、Z=3599`抓取，A箱按`X=26500、Z=4299`抓取。5cm仍约1029脉冲，B/C/A释放高度分别约为`1780/2570/3270`；这些实物高度与旧标定一致。

### XY点位连续导航

“XY点位导航”页只负责把夹爪移动到八个物理箱位正上方，不执行下降、抓取或放置。遥控器`1~5`选择数字区物理箱位1~5，`7/8/9`分别选择豆子箱A/B/C，`POWER`执行，`0`中止。这里的数字仍表示场地物理槽位，不是K230识别后的数字语义。物理点位映射如下：

| 遥控键 | 目标 | Y地标 | X脉冲 |
| --- | --- | --- | ---: |
| `1` | 数字区右侧1号箱 | 1/5共用地标 | 0 |
| `2` | 数字区底部右侧2号箱 | 2/3/4共用地标 | 8500 |
| `3` | 数字区底部中间3号箱 | 2/3/4共用地标 | 16300 |
| `4` | 数字区底部左侧4号箱 | 2/3/4共用地标 | 24400 |
| `5` | 数字区左侧5号箱 | 1/5共用地标 | 33924 |
| `7` | 豆子A箱 | A/C共用地标 | 26500 |
| `8` | 豆子B箱 | B独立地标 | 16300 |
| `9` | 豆子C箱 | A/C共用地标 | 6700 |

首次运行要求底盘处于起点两侧挡板、X位于横梁中点、Z触底且PB11被遮挡。选择目标后按`POWER`，程序先把Z升到最高点`Z=0`，再寻找X右端PB11并建立`X=0`，随后从起点直接规划到所选目标。到站后页面保存实际目标箱和Y地标；选择下一个目标并再次按`POWER`即可从当前位置继续，不重新回零。

该页统一调用正式导航器并按空爪规划。A/C与B之间、数字区侧箱与底部横排箱之间属于同区相邻移动，只经过二者之间的地标，不回起点；豆子区与数字区互相切换时，同侧目标沿左或右安全通道直达，异侧目标才在两障碍之间执行平滑换边，中线箱自动选择代价较低的一侧。每组中途挡板单侧触发时只锁存事件、四轮不停；另一侧确认同组挡板后保持现有速度连续通过，不清零速度环，目标地标才让四轮同时停车。单侧锁存超过全局允许时差仍未完成双侧确认会进入恢复或故障。Z在全部Y/X运动中始终保持`Z=0`。

该页的正常到站完全由PD8/PB12目标挡板触发。因为“1/5号箱地标→2/3/4号箱地标”的实测距离仍缺失，此页不使用编码器距离窗口判断，编码器到达任何数值都不会正常停车；每跨一组挡板仅设置7秒保守超时。中途按`0`、退出页面、急停、路线超时或限位故障会停止全部运动并作废当前位置，必须把实物恢复到规定起点姿态后重新建立参考，禁止从半途沿用旧地标。

### 数字区放豆演示页

“放豆控制”页用于实机演示数字区底部左、中、右三个横排箱位的连续放豆动作。该页采用人工确认起始姿态：启动前由操作者确保X轴位于右端`X=0`、Z轴位于最高点`Z=0`；按`POWER`后程序直接将当前脉冲坐标写为X=0、Z=0，不再要求先进入X/Z标定页面。若PB11因X右端挡片而处于遮挡，程序会将当前遮挡认领为X右端限位，并只允许X沿正方向离开。

此放宽仅用于人工看护下的Demo。若实际姿态不是X右端和Z最高点，软件仍会错误地把当前位置当作零点，可能导致箱位错位或碰撞；正式自动比赛流程仍必须使用传感器回零确认，不能沿用人工假定。

- `POWER`：开始、暂停或继续演示。
- `0`：立即中止演示，停止X/Z轴，并让旋转舵机恢复规定0度、夹爪恢复闭合10度。
- `VOL-/VOL+`或板载翻页键：离开演示页时自动中止演示。

每个箱位的投放高度改为`Z=3609`，对应旧坐标3300的同一实物高度；正式载豆任务中的旋转舵机以50°/s缓转，完成后稳定200ms才下降。

夹爪被豆子阻挡时，普通位置舵机无法反馈实际闭合角度，持续命令10度只能提供保持力，不能保证豆子不会滑落。机械上应优先采用软硅胶防滑面、弹簧顺应连接和能托底的半封闭爪形，避免仅靠两片硬爪摩擦夹持散料。软件抓取流程后续采用“闭合保持 -> 小幅试抬 -> 再次保持闭合 -> 正式抬升”，并预留舵机电流或指端压力检测接口；没有力反馈时必须限制堵转保持时间，防止舵机过热。

### 正式比赛运行

OLED“比赛运行”页是整场自动任务的唯一入口。把底盘放在起点双侧挡板、X放在横梁中点、Z触底并确认PB11遮挡后，按遥控器`POWER`启动；按`0`立即中止。页面持续显示任务序号、A/B/C到识别数字箱的映射、当前阶段、当前/目标离散地标及X/Y/Z坐标有效性，故障时改为显示具体故障类别。

正式流程只在开局执行一次Z抬升和X右端光电回零，随后复用视觉路线锁存物理1～5号数字槽和A/B/C豆子槽。数字物理顺序固定为“1右侧、2底右、3底中、4底左、5左侧”；豆子顺序固定为“A左、B中、C右”。数字路线从右下物理2号箱开始，X轴依次移动采集物理3、4号箱，侧向姿态采集物理5号箱，再用数字1～5的唯一性排除补齐物理1号箱。A/B按相机扫描姿态分别投票；A/B为不同豆类时用三类唯一性补齐C。首次结果缺失或冲突时仅进行一轮2秒定点补扫，补扫后仍不完整或存在重复语义就停止，不猜测继续。

豆子识别完成后保留当前位置为豆子识别地标，直接开始抓取，不返回起点重新出发。任务按物理`A -> C -> B`执行；串口语义`H/L/B`分别表示黄豆/绿豆/白芸豆，并固定投到数字1/2/3。载豆投放前旋转舵机以`50°/s`非阻塞缓转，90°约1.8秒，完成后稳定200ms才允许Z下降到3609。所有底盘运输要求Z=0且剩余脉冲清零。

同区移动按9组挡板的离散差值直接计数。抓取后只有夹爪闭合、Z=0且剩余脉冲清零才置为`LOADED`；放豆、回顶和工具复位完成后才置为`EMPTY`。最终Z触底重建为`6093`。

单侧光电超时或路线总超时首次发生时，底盘先停车300ms，再以15 RPM进行一次最长5秒的受控恢复：单侧落后时只驱动落后侧补齐同组挡板，两侧计数一致但尚未到站时双侧沿原方向低速寻找下一组挡板。只有重新得到双侧同组确认后才继续并更新Y地标；同一导航腿不重复恢复。急停、恢复失败、坐标无效、步进未到目标或PB11终点确认失败仍会停止底盘和机构并作废坐标，禁止盲走。

## 8. FreeRTOS 任务

当前任务来自 `Crane_rtos.ioc`：

| 任务 | 入口 | 优先级 | 栈配置 |
| --- | --- | ---: | ---: |
| APP_CTRL | `StartAppCtrlTask` | 24 | 192 words |
| DC_MOTOR | `StartDcMotorTask` | 32 | 256 words |
| K230_RX | `StartK230RxTask` | 24 | 256 words |
| INPUT_EVT | `StartInputEvtTask` | 32 | 192 words |
| OLED | `StartOledTask` | 8 | 256 words |
| IR_REMOTE | `StartInfraredTask` | 24 | 192 words |

FreeRTOS 动态堆当前为 12288 字节。底盘任务以 10ms 周期运行；应用控制、OLED 和红外任务使用绝对周期延时，避免长期漂移。步进换向状态机已合并进应用控制任务，减少一个常驻任务及其栈开销。`K230_RX`任务负责USART2 DMA环形缓冲、ASCII语义解析和单字节模型切换；K230持续发送时DMA始终接收，到点采集由业务层清空旧结果并开启本地投票窗口。限位和急停的硬件中断只做快速转发，消抖和业务处理在任务/安全模块中完成。

## 9. K230 运行文件

K230 Python 程序不参与 STM32 CMake 构建。K230 端需要单独准备：

```text
/sdcard/number/main.py
/sdcard/number/deploy_config.json
/sdcard/number/<kmodel文件>
```

模型路径、类别、输入尺寸和阈值由 `deploy_config.json` 决定。K230 的串口编号属于 K230 自己的硬件定义，不等同于 STM32 的 USART 编号；当前物理连接对应 STM32 USART2。

## 10. 新开发者建议流程

1. 克隆仓库并确认 Git、CMake、Ninja、ARM GCC 都能在命令行执行。
2. 使用 `cmake --preset Debug` 和 `cmake --build --preset Debug` 编译。
3. 打开 `Crane_rtos.ioc`，确认生成配置和引脚标签，再生成代码并重新编译。
4. 不接电机时先烧录，确认 OLED、按键、串口和限位输入。
5. 单独验证 Z 轴、舵机、M1，再逐步接入 M2 至 M4。
6. 校准编码器每圈计数、极性、底盘直行和光电限位位置。
7. 最后接入 K230 视觉结果、路线参数和自动比赛状态机。
# 摄像头俯仰舵机

- PWM 信号：`PB8 / TIM4_CH3`
- 定时器：`PSC=71`、`ARR=19999`，计数分辨率 `1us`，周期 `20ms`
- 平视位置：`CCR=1700`
- 俯视位置：`CCR=2500`
- 上电默认：平视
- 蜂鸣器因 PB8 改作 PWM，信号线迁移到 `PE6`
- 舵机必须使用独立的合适电源供电，并与 STM32、红外接收模块共地
- 控制接口位于 `camera_tilt.h`：`CameraTilt_SetLevel()`、`CameraTilt_SetDown()`、
  `CameraTilt_SetPulseUs()`

## 光电传感器预留

- 传感器1：`PB11 / EXTI11`
- 传感器2：`PB12 / EXTI12`
- 传感器3：`PD8 / EXTI8`

底盘两侧光电门按世界坐标方向固定定义：靠近场地右上角坐标原点的一侧接`PD8`（业务名`PHOTO_SENSOR_CHASSIS_ORIGIN_SIDE`），另一侧即远离原点的一侧接`PB12`（业务名`PHOTO_SENSOR_CHASSIS_FAR_SIDE`）。路线与到站代码必须使用这两个方向名称，不使用观察者视角下容易变化的“左/右”名称。`PB11`继续作为X/Z轴共用光电门（业务名`PHOTO_SENSOR_SHARED_XZ`）。

底盘手动调试已启用两侧独立光电到站停车：原点侧`PD8`只控制M1/M2，远端侧`PB12`只控制M3/M4。每次按`POWER`时不检查当前光电电平，四台电机始终允许启动；当前已遮挡的一侧先等待传感器恢复0，当前无遮挡的一侧立即布防。之后PD8下一次变为1时只将M1/M2输出清零，PB12下一次变为1时只将M3/M4输出清零，另一侧继续运行以完成机械对齐；两侧都触发后底盘整体停止。

底盘OLED页中`M12/M34`的1表示该侧仍在运行，0表示该侧已停车；`原/远`分别显示PD8和PB12原始电平；`防`后两位依次表示M1/M2侧和M3/M4侧是否已经布防。该功能用于测量两侧独立停车惯性，实际停车距离仍受车速、负载、地面和机械制动能力影响。
- 三路均为上拉输入、上升沿和下降沿中断
- `PhotoSensor_GetState()`返回GPIO原始电平；各传感器的遮挡极性按实测配置
- `PB13/PB14` 没有用于传感器中断，因为 EXTI13/14 已分配给
  `PD13/PD14` 的底盘编码器
- 三路均记录实时电平和变化标志；传感器2、3暂不执行运动控制
- 传感器1（`PB11`）实测为无遮挡低电平、遮挡高电平，作为X/Z两轴共用方向限位
- X轴和Z轴复用的是同一个PB11光电门，不是两套独立限位；机械设计和路线均不允许两个轴的挡片同时遮挡光束
- 驱动层强制X/Z运动互斥：任一轴正在输出脉冲时，另一轴的启动请求会被拒绝，保证光电门触发归属唯一
- 任一轴运动时触发PB11，最迟在10ms控制周期内记录触发轴和方向并停止该轴
- 遮挡期间仅允许触发轴沿相反方向脱离，原方向和另一轴均禁止启动
- 光束恢复后自动解除方向限位；上电时已经遮挡且没有触发记录时，两轴均禁止启动
- Z轴正常停止或触发光电门时仅关闭PUL脉冲，TB6600的ENA保持低电平以提供静态保持转矩
- X轴正常停止仍释放驱动；Z轴正常停止、任务故障和软件急停均保持TB6600使能，防止夹爪自由下坠。彻底断能仅保留给上电关闭或明确维护流程。
- Z轴保持会持续产生线圈电流和热量，应根据负载调整TB6600电流档位并检查电机温升
- `StepperAxis_MovePulses(axis, pulse_count, reverse)`可非阻塞输出指定数量的步进脉冲
- TIM1捕获比较中断在每个完整脉冲下降沿计数，达到目标后自动停止；X轴释放、Z轴进入保持
- 可通过`StepperAxis_GetCommandedPulses()`、`GetCompletedPulses()`和`GetRemainingPulses()`记录标定数据
- 指定脉冲运动中若触发光电门或安全停机，本次运动会被取消，已完成计数保留供诊断

### 步进轴实机标定（2026-07-26）

| 轴 | 实测基准 | 脉冲数 | 换算结果 | 建议安全工作行程 |
| --- | --- | ---: | ---: | ---: |
| X轴 | 两端光电门之间约169.3cm | 33924 | 约200.38脉冲/cm，0.04991mm/脉冲 | 33424脉冲，预留500脉冲约2.50cm |
| Z轴 | 夹爪触底遮挡光电门至新最高点约29.6cm | 6093 | 约205.84脉冲/cm，0.04858mm/脉冲 | 5893脉冲，上端预留200脉冲约0.97cm |

- `app_config.h`集中保存上述标定值，路线规划优先使用安全工作行程，不要直接使用机械全行程。
- 单次`StepperAxis_MovePulses()`指令不得超过对应轴的实测完整行程，超出时返回`HAL_ERROR`且不会启动。
- 当前脉冲累计值是软件输出计数，上电后不是可信绝对坐标；可先在“回零控制”页完成X/Z初始化，只有PB11确认触底和X右端零点后，绝对坐标才可信。
- X轴两端光电门和Z轴触底光电门继续承担硬限位。调试新路线时应先低速、短距离运行，再逐步扩大行程。
- Z轴停止后保持使能以防夹爪下滑；任务故障和软件急停同样保持Z与夹爪当前状态，长期保持时检查步进电机和TB6600温升。

## K230视觉通信协议

- STM32 USART2：`PA2 TX`、`PA3 RX`，`115200 8N1`，RX/TX均使用DMA
- K230 UART1：`IO40 TX`、`IO41 RX`，与STM32交叉连接并共地
- K230无需请求，每100ms自动发送当前最佳结果的单个ASCII字节；未识别时不发送
- 数字结果为`'1'~'5'`(`0x31~0x35`)；绿、黄、白豆分别为`'L'/'H'/'B'`(`0x4C/0x48/0x42`)
- STM32发送`'N'`(`0x4E`)切数字模型，发送`'B'`(`0x42`)切豆子模型；K230预热完成后返回`'n'`(`0x6E`)或`'b'`(`0x62`)确认
- DMA回调只把字节写入512字节环形缓冲；`K230_RX`任务持续排空缓冲、过滤非法字符并发布最新语义
- 协议不再携带帧头、长度、序号、CRC、置信度或画面坐标；STM32为兼容现有上层快照结构在内部生成递增序号，并填入`100%`和`(320,240)`占位值
- 由于未识别时K230不发送心跳，“暂无识别结果”不再被误判为离线；串口错误或模型切换超时才标记离线
- OLED五个数字槽和三个豆子槽使用路线当前物理姿态归槽，不再使用K230画面坐标
- 视觉页待机时按遥控器`1`向K230发送`N`并选择数字识别，按`2`发送`B`并选择豆子识别；OLED同页显示切换中/就绪/失败、在线状态和最新收到的`1~5/L/H/B`字符。`POWER`启动/停止完整视觉路线Demo，`0`立即停止Demo
- 模型切换期间丢弃旧模型在途字符，收到小写确认后清除旧结果并接收新模型字符

### 视觉路线Demo

`vision_route_demo.c/.h`用于独立验证“初始化定位 -> 数字识别 -> 豆子识别 -> 固定抓A -> 绕障投放物理4号箱 -> 返回起点”整条链路。进入OLED视觉页并按`POWER`后，非阻塞状态机会连续执行完整路线；再次按`POWER`或按`0`均可中止。

1. 按比赛起始姿态把Z触底位置认作`6093`脉冲，抬升约`1544`脉冲到离地约75mm的识别安全高度（绝对`Z≈4549`）。
2. X轴向右端寻找共用光电门并建立`X=0`，换向等待300ms后移动`8500`脉冲到物理2号箱对应位置。
3. 底盘从起点以`50RPM`经过下障碍上侧挡板完成两侧对齐，再自动前往数字识别挡板。
4. 到达数字识别点后，摄像头保持平视、旋转舵机为规定`0°`；调试模式依次到达`X=8500/16300/24400`对应物理2/3/4号箱，每点停稳400ms后清空旧结果，在本地1秒窗口内统计K230持续字符。
5. 横排扫描完后，旋转舵机转到规定`54°`只采集物理5号箱，物理1号箱由数字1～5唯一性排除得到；随后转到`180°`，底盘从数字识别点沿每组已标定挡板以`50RPM`连续分段运行至豆子识别点。
6. 到达豆子识别点后摄像头俯视，先以规定`180°`观察A箱，再转到`194°`观察B箱，每个姿态停留约1秒。A/B识别为两种不同豆类后，用三类唯一性补齐C。
7. 数字区始终显示5个固定框，豆子区始终显示3个固定框。K230结果实时填入；结果缺失或冲突会显示警告并补扫，但本次固定抓放测试不依赖识别语义。
8. 豆子采集结束后从当前豆子识别地标直接前往A箱，按`X=26500、Z=4299`抓取；随后使用跨区导航强制经过起点挡板并完成X换边。
9. 到达物理4号箱后按`X=24400、Z=3609`放置。物理2/3/4使用`90°`竖向投放；物理1/5保持`0°`。
10. 投放完成后返回起点，X回到中点`16962`，Z下降触发PB11并重建`Z=6093`后结束。

视觉路线分别保存数字区5个框和豆子区3个框的稳定投票结果。OLED视觉页采用单屏总览，第二行始终显示5个数字槽，第三行始终显示3个豆子槽，识别过程中实时填入且互不覆盖；底行显示当前路线状态和K230离线提示。路线完成后两组结果继续保留在同一屏，不需要按键切换页面。只有再次按`POWER`启动新一轮视觉路线时才清空两组旧结果。

视觉调试页与正式比赛任务共用同一套定点ASCII多数票：先严格等待`n/b`模型就绪确认，再到达对应X点位、停稳400ms并清空旧结果。每收到一个合法新数字字符就加1票，OLED当前物理框立即显示实时领先值；平票时保留当前值避免框内闪空。前面物理框已锁定的数字不能再被后续框选中，离开点位后锁存最终领先值；若整个窗口只收到已占用数字，不伪造结果，进入既有的一次定点补扫。豆子A/B姿态也在各自采集窗口内分别统计`L/H/B`，保存出现次数最多的类别，并用唯一性排除补齐C箱。正式比赛不再使用连续横移归槽，避免实机已调通的Demo逻辑与比赛逻辑不一致。

视觉调试Demo的固定A到4号箱抓放不以识别结果作为启动条件；识别缺失或重复只保留警告。路线超时、光电恢复失败、急停/限位故障、抓放动作失败或X/Z回零异常仍会立即进入故障并停机。正式比赛任务继续读取可信锁存结果动态规划，不执行这条固定测试。

每个Y路线段都从一对挡板出发并在下一对挡板确认。PD8、PB12任一侧先触发时只锁存事件，不停止对应轮组；另一侧超过1秒仍未触发时判定为漏检或严重歪斜，立即停止整车并令路线失败。每侧触发时还会读取本侧两个编码器的平均行程，当前宽松接受范围为理论值的`45%~165%`：低于下限的遮挡视为当前挡板残留、停车滑移回弹或边沿抖动，直接忽略且不推进路线；高于上限才作为漏站或编码器异常报错。范围以内一律以光电门位置为准。OLED行进页中的“到”是已经锁存本组事件的侧掩码，“行”在双侧确认前仍保持两侧运行；位0对应M1/M2侧，位1对应M3/M4侧。

视觉页仍可独立运行巡检路线和查看锁存结果；“比赛运行”页会读取同一份锁存地图生成A/B/C正式抓放任务，不再维护第二套扫描结果。
